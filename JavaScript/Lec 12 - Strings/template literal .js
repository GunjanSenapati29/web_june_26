const a = 10
const b = 20

const str = 'the value of a is '+a+" the value of b is "+b+" sum of a & b is "+(a+b)
console.log(str) 

const templateString = `Using template literal = a value is ${a}, b value is ${b}, sum of a & b is ${a+b}`
console.log(templateString)